name: Release

on:
  push:
    tags: ["v*.*.*"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write

jobs:
  Release-Build-Linux:
    runs-on: ubuntu-22.04
    container: ${{ matrix.container }}
    permissions:
      contents: read
      attestations: write
      id-token: write
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            container: ghcr.io/cross-rs/x86_64-unknown-linux-gnu:0.2.5
          - target: aarch64-unknown-linux-gnu
            container: ghcr.io/cross-rs/aarch64-unknown-linux-gnu:0.2.5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Cargo build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Show version
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: ./target/${{ matrix.target }}/release/wparse --version

      - name: Archive
        run: |
          mkdir -p artifacts
          cp -av target/${{ matrix.target }}/release/wparse artifacts/
          cp -av target/${{ matrix.target }}/release/wpgen artifacts/
          cp -av target/${{ matrix.target }}/release/wprescue artifacts/
          cp -av target/${{ matrix.target }}/release/wproj artifacts/
          tar -czf ${{ github.event.repository.name }}-${{ github.ref_name }}-${{ matrix.target }}.tar.gz artifacts

      - name: Attest Build Provenance
        uses: actions/attest-build-provenance@v3
        continue-on-error: true
        with:
          subject-path: ${{ github.event.repository.name }}-${{ github.ref_name }}-${{ matrix.target }}.*

      - uses: actions/upload-artifact@v6
        with:
          name: ${{ github.event.repository.name }}-${{ github.ref_name }}-${{ matrix.target }}
          path: ${{ github.event.repository.name }}-${{ github.ref_name }}-${{ matrix.target }}.*
          if-no-files-found: error
          retention-days: 7

  Release-Build-Mac:
    runs-on: macos-14
    permissions:
      contents: read
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: aarch64-apple-darwin

      - name: Cargo build
        run: cargo build --release --target aarch64-apple-darwin

      - name: Show version
        run: ./target/aarch64-apple-darwin/release/wparse --version

      - name: Archive
        run: |
          mkdir -p artifacts
          cp -av target/aarch64-apple-darwin/release/wparse artifacts/
          cp -av target/aarch64-apple-darwin/release/wpgen artifacts/
          cp -av target/aarch64-apple-darwin/release/wprescue artifacts/
          cp -av target/aarch64-apple-darwin/release/wproj artifacts/
          tar -czf ${{ github.event.repository.name }}-${{ github.ref_name }}-aarch64-apple-darwin.tar.gz artifacts

      - name: Attest Build Provenance
        uses: actions/attest-build-provenance@v3
        continue-on-error: true
        with:
          subject-path: ${{ github.event.repository.name }}-${{ github.ref_name }}-aarch64-apple-darwin.*

      - uses: actions/upload-artifact@v6
        with:
          name: ${{ github.event.repository.name }}-${{ github.ref_name }}-aarch64-apple-darwin
          path: ${{ github.event.repository.name }}-${{ github.ref_name }}-aarch64-apple-darwin.*
          if-no-files-found: error
          retention-days: 7

  Release:
    needs: [Release-Build-Linux, Release-Build-Mac, Docker-Build]
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Ensure artifacts dir exists
        run: mkdir -p artifacts

      - name: Download Artifact
        uses: actions/download-artifact@v7
        with:
          path: artifacts
          merge-multiple: true

      - name: Release
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b
        with:
          name: ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          body: |
            Welcome to this new release of WarpParse ${{ github.ref_name }}!

            ## 📦 Installation

            ### Binary Downloads
            Download the pre-built binaries for your platform from the assets below.

            ### Docker Image
            Pull and run the Docker image:
            ```shell
            # Pull the specific version
            docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}

            # Or pull the latest version
            docker pull ghcr.io/${{ github.repository }}:latest

            # Run the container
            docker run --rm ghcr.io/${{ github.repository }}:${{ github.ref_name }} --help
            ```

            ## 🔐 Verification

            All artifacts are signed with this repos identity using Sigstore.
            You can verify the signatures using the `GitHub` CLI.

            ```shell
            gh attestation verify --owner ${{ github.repository_owner }} <my-artifact>
            ```
          token: ${{ secrets.GITHUB_TOKEN }}
          prerelease: ${{ contains(github.ref, '-pre') }}
          files: artifacts/*

  Docker-Build:
    needs: [Release-Build-Linux]
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download amd64 artifact
        uses: actions/download-artifact@v7
        with:
          name: ${{ github.event.repository.name }}-${{ github.ref_name }}-x86_64-unknown-linux-gnu
          path: docker-build/amd64-tar

      - name: Download arm64 artifact
        uses: actions/download-artifact@v7
        with:
          name: ${{ github.event.repository.name }}-${{ github.ref_name }}-aarch64-unknown-linux-gnu
          path: docker-build/arm64-tar

      - name: Extract binaries
        run: |
          cd docker-build
          # Extract amd64
          cd amd64-tar && tar -xzf *.tar.gz && cd ..
          mkdir -p amd64
          mv amd64-tar/artifacts/* amd64/
          chmod +x amd64/*
          rm -rf amd64-tar
          # Extract arm64
          cd arm64-tar && tar -xzf *.tar.gz && cd ..
          mkdir -p arm64
          mv arm64-tar/artifacts/* arm64/
          chmod +x arm64/*
          rm -rf arm64-tar
          # Copy Dockerfile
          cp ../Dockerfile .

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: docker-build
          file: docker-build/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v3
        continue-on-error: true
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
